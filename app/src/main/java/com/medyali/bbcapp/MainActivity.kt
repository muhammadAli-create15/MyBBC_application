package com.medyali.bbcapp

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
/*import android.widget.FrameLayout
import androidx.constraintlayout.widget.ConstraintLayout
import androidx.fragment.app.Fragment
import com.medyali.bbcapp.Fragments.*/

class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
     //Declare the instances of each fragment
       /* val homeFragment=HomeFragment()
        val newsFragment=NewsFragment()
        val sportsFragment=SportsFragment()
        val worklifeFragment=WorkLifeFragment()
        val weatherFragment=WeatherFragment()
        val aboutFragment=AboutFragment()
        //Set the initial fragment that shows when we create the app

       setCurrentFragment(homeFragment)*/
        //Respond to the clicks on our different menu items and switch to the corresponding fragments
      // var bottomNavigationView=findViewById<ConstraintLayout>(R.id.bottomNav)
       // bottomNavigationView.
    //}
 /*   private fun setCurrentFragment(fragment: Fragment) =
        supportFragmentManager.beginTransaction().apply {
            replace(R.id.container,fragment)
            commit()*/
        }
}




